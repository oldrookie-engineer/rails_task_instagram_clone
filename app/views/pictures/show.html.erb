<h1>投稿詳細</h1>
<p id="notice"><%= notice %></p>
<p>
  <strong>画像:</strong><br>
  <%= image_tag @picture.image.url %>
</p>
<%= link_to '編集', edit_picture_path(@picture) if @picture.user_id == current_user.id %>
<%= link_to '投稿一覧', pictures_path %>
